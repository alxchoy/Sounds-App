export interface Animal {
    // El interface lo usamos para declarar las propiedades que tendra cada animal
    nombre: string;
    imagen: string;
    audio: string;
    duracion: number;
    reproduciendo: boolean;
}